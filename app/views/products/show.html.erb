<h1>Blend Name: <%= @product.name %></h1>

<p>Price: $<%= @product.price %></p>

<ul>
  <li><%= link_to "Edit", edit_product_path(@product) %></li>
  <li><%= link_to "Delete", product_path(@product),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %></li>
</ul>

<%= render @product.variety %>